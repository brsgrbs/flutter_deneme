import 'package:flutter/material.dart';

class ColumRowLearn extends StatelessWidget {
  const ColumRowLearn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text('data'),
    );
  }
}
